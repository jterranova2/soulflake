# Room: Context

**Live tonight:** arm not shipped. 09-15 My flake asks city and country on seat beat 3 (`soulflake.place`) after name / show-up / age. That is not this arm.
**When carved:** My flake arm `context`
**Lead:** Facts as you name them. Skip allowed. One field at a time.

- Name field (What should we call you?) — also the birth-name on My flake. Live already asks this before any arm.
- Faith, as lived: Skip / Christian / Other faith / None / unsure
- Where you are, roughly: Skip / West Coast US / Other US / Outside the US

Skip is a real answer. Do not invent a faith or place.

Name lives on the profile object (`profile.name`), not only inside `answers.context`. Tonight it lives in `soulflake.name` until profiles.v1 returns.

Do not treat the free-text city-and-country seat as the rough-place list above.
