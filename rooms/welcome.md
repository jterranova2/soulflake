# Room: Welcome

**Live files:** `index.html` (root) and `Soulflake.html` (same page tonight)
**Job:** First visit. Mood, not a file desk. Layer 0, plus the thin Get started stub added 09-10.

## Must be true

- Wordmark Soul frost, flake cyan. Meet your Soulflake.
- Hero is the live six-color dendrite SVG (arm-g0…g5, spin-bands + reverse spin-crystal). Not `Soulflake-Welcome-Crystal.jpg`. Size `min(288px, 74.4vw)`.
- Six rotating briefing lines from the room brief. First swap at 7s, then 11.5s, 1.5s fade each way.
- One button, twice: **Get started**, weight 500 (not Build your Flake, not Sign in).
- Pitch in this order. Inline SVGs, not frames 80–83 JPEGs:
  1. The hole — You walk in blurry.
  2. Six cuts, not a type — Traits. Taste. Bond. Values. Conflict. Context.
  3. It grows when you tell it — lopsided on purpose.
  4. Built to be used — Know yourself. Then be known. No score. Glue, grind, repair.
- Cue: Start with one arm.
- No Export. No Import. No name/password dock. No nav. Never Me. Never Others.
- Sound: `assets/audio/AUDIO-welcome.mp3` only. On unless `soulflake.sound` is `off`. Gain 2.2. First-gesture unlock. Off stays off. Button glows when on.
- Atmosphere that stays: moonlight-white + cyan rays (25% less opaque than the first ray pass), fog, vignette, breath, ground frost, slower snow.
- Atmosphere that does not come back: ice ring, glint sweep, halo, Frost Waltz file, commercial pitch JPEGs.

## Landed 09-10 that the 09-09 brief missed

- Get started is wired to a stub, not to a first question. `showMe()` adds `body.me` and `.hero-flake.me`, sets the headline to **My flake**, hides pitch / cue / rotate / bottom, leaves only g0 frost spines.
- Crystal JPEG is out. Vector flake is the mark.
- Sound bed is the louder local mix, not Kevin MacLeod Frost Waltz at 0.18.

## Not landed

- Get started does not name a profile, does not ask a question, does not grow an arm past frost.
- Arm-zoom frames 70–74 still missing.
